#sidebar
  %h1#logo
    = link_to 'DL', root_path
  = render 'layouts/who_is_dl'
  = render 'layouts/navigation'
  = render 'layouts/copyright'
  - if admin_signed_in?
    .admin_actions
      %p
        Admin Functions
      = link_to 'sign out', :sign_out
      = link_to '+ add post', :new_blog
